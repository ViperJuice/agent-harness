# Python build artifacts — never commit these (a stale build/ shadowed the version)
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]
.venv/

# CS-0.10c local-file LeaseStore: per-machine coordination state, never repo truth.
.consiliency/leases/
.dev-skills/handoffs/
.pytest_cache/
